name: (disabled) DoD Enforcer
on:
  workflow_dispatch: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This legacy workflow is disabled; DoD is enforced by PR Validate / evidence-dod."