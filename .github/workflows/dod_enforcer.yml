name: DoD Enforcer
on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  enforce:
    name: Enforce DoD Compliance
    runs-on: ubuntu-latest
    steps:
      - name: Delegate to pr-validate gate
        run: |
          echo "DoD is enforced by 'pr-validate / evidence-dod'. This legacy gate is a shim."
          exit 0