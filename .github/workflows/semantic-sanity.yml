name: semantic-sanity
on: [push, pull_request]
jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/semantic_eval.py
      - uses: actions/upload-artifact@v4
        with:
          name: quality.json
          path: reports/quality.json
