apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: planner
  namespace: hyper-swarm
  labels:
    app: planner
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/metric: concurrency
        autoscaling.knative.dev/target: '10'
        autoscaling.knative.dev/minScale: '0'
        autoscaling.knative.dev/maxScale: '30'
    spec:
      containers:
      - name: planner
        image: ghcr.io/hirakuarai/planner:0.2.0
        ports:
        - containerPort: 8003
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8003
          initialDelaySeconds: 2
          periodSeconds: 5
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: '1'
            memory: 512Mi
        envFrom:
        - configMapRef:
            name: planner-config
